

  <%= form_for @post do |f| %>
  <%= f.select (:category, ["Job", "Projects", "Other"], {}, { :class => 'input-field' })%>

    <%= f.text_field :title, placeholder: 'Title', :class => 'input-field' %>
    <%= f.text_area :body, placeholder: 'content...', :class => 'input-field' %>
    <%= f.submit "Save", :class => 'form-submit-button' %>
    <% end %>
  </div>
