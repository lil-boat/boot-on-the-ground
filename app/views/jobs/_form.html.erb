
  <%= form_for @job do |f| %>
    <%= f.text_field :title, placeholder: 'Job Title', :class => 'input-field' %>

    <%= f.collection_select(:company, Company.all, :id, :name, include_blank:true, :class => 'input-field') %>

    <%= f.submit "Save", :class => 'form-submit-button' %>
  <% end %>